# Speech recognition
vosk>=0.3.45
pyaudio>=0.2.14

# Audio playback
pygame>=2.0.0

# Text-to-speech
pyttsx3>=2.90

# Numerics (required by vosk, voice auth, intent model)
numpy>=1.24.0

# HTTP client (used by intent_api to call local FastAPI server)
requests>=2.28.0

# Intent API server (run separately: uvicorn Api.app:app)
fastapi>=0.100.0
uvicorn>=0.22.0
joblib>=1.3.0
scikit-learn>=1.3.0

# Optional: better voice biometric accuracy (fallback to numpy if not installed)
# resemblyzer

# Optional: neural intent encoding (fallback to TF-IDF if not installed)
# sentence-transformers>=2.2.0
