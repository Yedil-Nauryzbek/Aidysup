<!-- WpfApp1/Resources/Styles.xaml (как у тебя, без изменений) -->
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== Colors ===== -->
    <Color x:Key="C_WindowFill">#101427</Color>
    <Color x:Key="C_WindowBorder">#2A3B6A</Color>
    <Color x:Key="C_SidebarFill">#0F1630</Color>

    <Color x:Key="C_TextPrimary">#EAF0FF</Color>
    <Color x:Key="C_TextMuted">#9AA8C7</Color>

    <Color x:Key="C_AccentCyan">#9BB6FF</Color>
    <Color x:Key="C_Green">#2CFF9A</Color>

    <SolidColorBrush x:Key="WindowFill" Color="{StaticResource C_WindowFill}"/>
    <SolidColorBrush x:Key="WindowBorder" Color="{StaticResource C_WindowBorder}"/>
    <SolidColorBrush x:Key="SidebarFill" Color="{StaticResource C_SidebarFill}"/>

    <SolidColorBrush x:Key="TextPrimary" Color="{StaticResource C_TextPrimary}"/>
    <SolidColorBrush x:Key="TextMuted" Color="{StaticResource C_TextMuted}"/>

    <SolidColorBrush x:Key="AccentCyan" Color="{StaticResource C_AccentCyan}"/>
    <SolidColorBrush x:Key="OnlineGreen" Color="{StaticResource C_Green}"/>

    <!-- ===== Effects ===== -->
    <DropShadowEffect x:Key="WindowShadow" BlurRadius="32" Opacity="0.45" ShadowDepth="10" Color="#000000"/>
    <DropShadowEffect x:Key="GlowLogo" BlurRadius="22" Opacity="0.55" ShadowDepth="0" Color="#9BB6FF"/>
    <DropShadowEffect x:Key="GlowGreen" BlurRadius="18" Opacity="0.70" ShadowDepth="0" Color="#2CFF9A"/>
    <DropShadowEffect x:Key="GlowPink" BlurRadius="22" Opacity="0.25" ShadowDepth="0" Color="#FF58B8"/>
    <DropShadowEffect x:Key="GlowViolet" BlurRadius="22" Opacity="0.18" ShadowDepth="0" Color="#7C5CFF"/>

    <!-- ===== NoiseBrush ===== -->
    <DrawingBrush x:Key="NoiseBrush" TileMode="Tile" Viewport="0,0,32,32" ViewportUnits="Absolute"
                  Viewbox="0,0,32,32" ViewboxUnits="Absolute" Opacity="0.75">
        <DrawingBrush.Drawing>
            <DrawingGroup>
                <GeometryDrawing Brush="#12FFFFFF">
                    <GeometryDrawing.Geometry>
                        <GeometryGroup>
                            <EllipseGeometry Center="4,6" RadiusX="1" RadiusY="1"/>
                            <EllipseGeometry Center="12,10" RadiusX="1" RadiusY="1"/>
                            <EllipseGeometry Center="22,4" RadiusX="1" RadiusY="1"/>
                            <EllipseGeometry Center="28,18" RadiusX="1" RadiusY="1"/>
                            <EllipseGeometry Center="6,24" RadiusX="1" RadiusY="1"/>
                            <EllipseGeometry Center="16,28" RadiusX="1" RadiusY="1"/>
                            <EllipseGeometry Center="26,28" RadiusX="1" RadiusY="1"/>
                        </GeometryGroup>
                    </GeometryDrawing.Geometry>
                </GeometryDrawing>
            </DrawingGroup>
        </DrawingBrush.Drawing>
    </DrawingBrush>

    <!-- ===== Typography ===== -->
    <Style TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="FontSize" Value="14"/>
    </Style>

    <Style x:Key="H1" TargetType="TextBlock">
        <Setter Property="FontSize" Value="26"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Opacity" Value="0.92"/>
    </Style>

    <Style x:Key="MenuText" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
        <Setter Property="Opacity" Value="0.92"/>
    </Style>

    <!-- ===== Menu Button ===== -->
    <Style x:Key="MenuItemButton" TargetType="Button">
        <Setter Property="Height" Value="52"/>
        <Setter Property="Margin" Value="0,0,0,12"/>
        <Setter Property="Background" Value="#00000000"/>
        <Setter Property="BorderBrush" Value="#1F9BB6FF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            CornerRadius="12"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#1AFFFFFF"/>
                            <Setter TargetName="bd" Property="BorderBrush" Value="#3A9BB6FF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#28FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ===== Chrome Buttons ===== -->
    <Style x:Key="ChromeButton" TargetType="Button">
        <Setter Property="Width" Value="44"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Background" Value="#0FFFFFFF"/>
        <Setter Property="BorderBrush" Value="#22FFFFFF"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="bd"
                            CornerRadius="10"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#22FFFFFF"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="bd" Property="Background" Value="#35FFFFFF"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
