<!-- WpfApp1/Views/LocalModePickerWindow.xaml -->
<Window x:Class="WpfApp1.Views.LocalModePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Choose Action"
        Height="520"
        SizeToContent="Width"
        MinWidth="197"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <!-- Thin dark scrollbar for panel lists -->
        <Style x:Key="ThinThumb" TargetType="Thumb">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border CornerRadius="3" Background="#2A3A55" Margin="1,2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ThinScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="5"/>
            <Setter Property="MinWidth" Value="5"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="Transparent">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ThinThumb}"/>
                                </Track.Thumb>
                            </Track>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PanelScrollViewer" TargetType="ScrollViewer">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <ScrollContentPresenter Grid.Column="0" CanContentScroll="{TemplateBinding CanContentScroll}"/>
                            <ScrollBar Grid.Column="1"
                                       Style="{StaticResource ThinScrollBar}"
                                       Orientation="Vertical"
                                       Value="{TemplateBinding VerticalOffset}"
                                       Maximum="{TemplateBinding ScrollableHeight}"
                                       ViewportSize="{TemplateBinding ViewportHeight}"
                                       Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- Outer frame -->
    <Border CornerRadius="14" Background="#0B1120" BorderBrush="#1E3050" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="50" ShadowDepth="0" Opacity="0.80"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="48"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- ── HEADER ── -->
            <Grid Grid.Row="0" Margin="16,0,10,0"
                  MouseLeftButtonDown="Header_MouseDown" Cursor="SizeAll">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Choose Action" FontSize="14" FontWeight="SemiBold"
                               Foreground="#D8E4FF"/>
                    <TextBlock x:Name="SlotLabel" FontSize="10" Foreground="#2E3E58"
                               Margin="0,2,0,0"/>
                </StackPanel>
                <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                        Content="✕" Width="28" Height="28"
                        Click="CloseBtn_Click" Cursor="Hand"
                        FocusVisualStyle="{x:Null}">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" CornerRadius="6" Background="Transparent"
                                    Width="28" Height="28">
                                <TextBlock Text="{TemplateBinding Content}" FontSize="12"
                                           Foreground="#3A4A6A"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#18253A"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>

            <Border Grid.Row="0" Height="1" Background="#111E2E" VerticalAlignment="Bottom"/>

            <!-- ── CASCADING PANELS ── -->
            <StackPanel Grid.Row="1" Orientation="Horizontal">

                <!-- Level 1 (always visible) -->
                <ScrollViewer Width="185" Style="{StaticResource PanelScrollViewer}"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <StackPanel x:Name="Level1Panel" Margin="6,6,4,8"/>
                </ScrollViewer>

                <!-- Separator L1→L2 -->
                <Border x:Name="Sep12" Width="1" Background="#111E2E"
                        Margin="0,10" Visibility="Collapsed"/>

                <!-- Level 2 -->
                <ScrollViewer x:Name="Level2Scroll" Width="185"
                              Style="{StaticResource PanelScrollViewer}"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Visibility="Collapsed">
                    <StackPanel x:Name="Level2Panel" Margin="6,6,4,8"/>
                </ScrollViewer>

                <!-- Separator L2→L3 -->
                <Border x:Name="Sep23" Width="1" Background="#111E2E"
                        Margin="0,10" Visibility="Collapsed"/>

                <!-- Level 3 -->
                <ScrollViewer x:Name="Level3Scroll" Width="185"
                              Style="{StaticResource PanelScrollViewer}"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled"
                              Visibility="Collapsed">
                    <StackPanel x:Name="Level3Panel" Margin="6,6,4,8"/>
                </ScrollViewer>

            </StackPanel>
        </Grid>
    </Border>
</Window>
